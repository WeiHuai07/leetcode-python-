class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        for i in range(nums.count(0)):
        #python数组中count函数的意思是计算nums中0的个数，然后开始遍历#
            nums.remove(0)
            nums.append(0)
